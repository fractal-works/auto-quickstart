// Filled in by the onboarding agent when it wires the site request bar.
window.AUTO = {
  // Webhook ingest URL for site requests.
  webhookUrl: "",
  // Project sessions page, linked after a request is handed off.
  sessionsUrl: "",
};
