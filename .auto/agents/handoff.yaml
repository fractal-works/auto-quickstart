name: handoff
imports:
  - "@auto/handoff@latest/agents/handoff.yaml"
  # The onboarding agent adds ../fragments/site-handoff-trigger.yaml here
  # and ../fragments/site-request-watch.yaml to .auto/agents/onboarding.yaml
  # when it wires the site request bar (both in the same wiring PR).
